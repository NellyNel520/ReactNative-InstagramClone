import { View, TextImage, StyleSheet, TouchableOpacity } from 'react-native'
import React from 'react'

const Buttons = () => {
  return (
    <View>
      <Text style={{color: 'white'}}>Buttons</Text>
    </View>
  )
}

export default Buttons